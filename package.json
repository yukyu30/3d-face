{
  "name": "3d-face",
  "version": "1.0.0",
  "description": "",
  "main": "dist-electron/electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "electron:build": "vite build && tsc -p tsconfig.electron.json && electron-rebuild -m . -f",
    "electron:dev": "vite build && tsc -p tsconfig.electron.json && electron-rebuild -m . -f && electron .",
    "test": "npm rebuild better-sqlite3 && vitest run",
    "test:watch": "npm rebuild better-sqlite3 && vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@electron/rebuild": "^4.0.3",
    "@types/better-sqlite3": "^7.6.13",
    "@types/three": "^0.183.1",
    "electron-builder": "^26.8.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@mediapipe/camera_utils": "^0.3.1675466862",
    "@mediapipe/face_mesh": "^0.4.1633559619",
    "@vladmandic/face-api": "^1.7.15",
    "better-sqlite3": "^12.8.0",
    "electron": "^41.0.2",
    "three": "^0.183.2"
  }
}
